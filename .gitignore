# Local operator configuration and runtime artifacts
ops/config.local.psd1
ops/logs/
ops/*.pid
ops/*.partial
ops/.test-runtime-*/
.runtime/
.tools/

# Local PDF corpus used by the knowledge-base validation. Keep the manifest,
# flow script, and report in Git, but do not distribute source papers.
validation/knowledge-papers/*.pdf

# =====================
# Personal / local-only artifacts — do NOT publish to GitHub
# =====================
.workbuddy/
.pi-glla/
实习/
output/
docs/
build_jinglianwen_report.py
reorder_jinglianwen_report.py
rewrite_after_chapter9.py
rewrite_competitor_section.py
ops/migrate-data-library.mjs

# Secrets / environment files
.env
.env.local
.env.*.local
